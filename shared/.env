# UNUSED — no PHP file in this repo requires shared/env.php or this file.
# Superseded by the root-level .env/env.php (which every migrated site uses).
# Slated for removal — see chat history. If you're re-adding a dependency on
# this file, rotate the DB credentials below first (they've sat in git since
# the initial commit).
# Shared PPC Darbar Configuration
API_URL=https://moneytreerealty.com/api/ppc-organic
API_TOKEN=3|XiCALsvIUjhLDhWWr7VxAMgDMARPRNy9ICnaUdqa65fcfada
DB_HOST=localhost
DB_USER=ppc_darbar
DB_PASS=PpcD4rb@r_2026!sEcUrE
DB_CHARSET=utf8mb4
